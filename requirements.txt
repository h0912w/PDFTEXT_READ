# Core PDF processing
pdfplumber>=0.10.0      # 텍스트 레이어 추출 (DIGITAL/HYBRID)
PyMuPDF>=1.23.0         # PDF → 이미지 렌더링 (Claude Code가 볼 수 있도록)

# Image processing (스캔 전처리용)
opencv-python>=4.8.0
Pillow>=10.0.0

# Output generation
openpyxl>=3.1.2
pandas>=2.0.0

# Numeric
numpy>=1.24.0

# ── 제거된 항목 ────────────────────────────────────────────────────────────
# pytesseract, easyocr → 불필요: OCR은 Claude Code가 직접 수행
# anthropic            → 불필요: 이 대화 자체가 LLM (Claude Code)
